# Speedtest-Meter

A simple way to use the [speedtest cli](https://www.speedtest.net/apps/cli) in docker, without installing the actual cli.

> [!WARNING]
> This tool accepts the `--accept-license` and `--accept-gdpr` flags by default, which automatically accept all license and GDPR terms.
